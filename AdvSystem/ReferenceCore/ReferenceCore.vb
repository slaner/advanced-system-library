Imports System.Runtime.InteropServices
Imports AdvSystem.ReferenceCore.Enumeration
Imports AdvSystem.ReferenceCore.Structure

Namespace ReferenceCore

    ''' <summary>
    ''' 라이브러리에서 사용되는 API들을 모아놓은 클래스입니다.
    ''' </summary>
    Friend MustInherit Class Api

    End Class

    ''' <summary>
    ''' 라이브러리에서 사용되는 구조체들을 모아놓은 클래스입니다.
    ''' </summary>
    Public MustInherit Class [Structure]

    End Class

    ''' <summary>
    ''' 라이브러리에서 사용되는 상수들을 모아놓은 클래스입니다.
    ''' </summary>
    Public MustInherit Class Enumeration

    End Class

    ''' <summary>
    ''' 라이브러리에서 사용되는 대리자 함수들을 모아놓은 클래스입니다.
    ''' </summary>
    Public MustInherit Class [Delegate]

    End Class

End Namespace